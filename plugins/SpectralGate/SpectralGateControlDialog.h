#ifndef LMMS_SPECTRALGATE_CONTROL_DIALOG_H#define LMMS_SPECTRALGATE_CONTROL_DIALOG_H#include "EffectControlDialog.h"#include <vector>class QPushButton;class QLineEdit;class QLabel;namespace lmms{class SpectralGateControls;namespace gui{class SpectralGateControlDialog : public EffectControlDialog{    Q_OBJECTpublic:    SpectralGateControlDialog(SpectralGateControls* controls);    ~SpectralGateControlDialog() override = default;public slots:    void updateButtonsFromModel();    void onButtonClicked();    void onRunIndexChanged(int index);        // NEW: Update the preset name labels when knobs turn    void updatePresetLabels();private:    SpectralGateControls* m_controls;    std::vector<QPushButton*> m_stepButtons;    QLineEdit* m_openFormulaEdit;    QLineEdit* m_closedFormulaEdit;        // NEW: Labels to display "Lowpass", "Vowel", etc.    QLabel* m_openPresetNameLabel;    QLabel* m_closedPresetNameLabel;        // Helper to get name from ID    QString getPresetName(int index);};} // namespace gui} // namespace lmms#endif